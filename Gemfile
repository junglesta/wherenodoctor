source 'https://rubygems.org'
ruby RUBY_VERSION

# IMPORTANT: these gems are the only ones that make sure the id in h1 is produced as intended, so stick with them...
gem 'jekyll', '3.1.6'
gem 'github-pages', '88'

# If you have any plugins, put them here!
group :jekyll_plugins do
   gem 'jekyll-sitemap'
   gem 'jekyll-seo-tag'
   gem 'html-proofer'
end
